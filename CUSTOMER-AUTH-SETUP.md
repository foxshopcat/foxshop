# حساب مشتریان FoxShop — احراز هویت فقط با ایمیل

این نسخه برای ثبت‌نام و ورود مشتریان از **ایمیل בלבד** استفاده می‌کند. کاربر ایمیل خود را وارد می‌کند، یک کد یک‌بارمصرف ۶ رقمی به همان ایمیل ارسال می‌شود و بعد از تأیید کد، در اولین ورود برای حسابش رمز عبور تعیین می‌کند. ورودهای بعدی با همان ایمیل و رمز انجام می‌شود.

## ارسال واقعی ایمیل

ارسال OTP در کد سمت Worker انجام می‌شود و به‌صورت واقعی از API سرویس Resend به آدرس ایمیل واردشده ارسال می‌گردد. برای فعال‌شدن ارسال، این سه Secret باید در Worker تنظیم شوند:

```bash
wrangler secret put AUTH_PEPPER
wrangler secret put RESEND_API_KEY
wrangler secret put AUTH_EMAIL_FROM
```

`AUTH_PEPPER` باید یک مقدار تصادفی و طولانی باشد. `RESEND_API_KEY` کلید API حساب Resend است و `AUTH_EMAIL_FROM` باید یک فرستنده/دامنه تأییدشده در Resend باشد. مستندات رسمی Cloudflare نیز استفاده از Resend در Workers و نگهداری API key در Secret را توضیح می‌دهد.

## جریان کاربر

1. کاربر ایمیل معتبر وارد می‌کند.
2. FoxShop یک OTP تصادفی ۶ رقمی ایجاد می‌کند.
3. خود OTP خام داخل D1 ذخیره نمی‌شود؛ نسخه هش‌شده آن ذخیره می‌شود.
4. ایمیل خوش‌آمدگویی حرفه‌ای شامل کد، زمان انقضا و پیام امنیتی به همان ایمیل ارسال می‌شود.
5. بعد از تأیید کد، Session امن HttpOnly ساخته می‌شود.
6. اگر اولین ورود باشد، صفحه حساب از کاربر می‌خواهد یک رمز حداقل ۸ کاراکتری تعیین کند.
7. از این به بعد ورود با ایمیل + رمز انجام می‌شود.

## محدودیت‌های امنیتی

Session cookie از نوع HttpOnly/Secure/SameSite است، OTP زمان انقضا و سقف تلاش دارد، درخواست OTP و ورود رمزی rate limit دارند، endpointهای تغییر‌دهنده Same-Origin/Fetch-Metadata را بررسی می‌کنند و رمزها با PBKDF2-HMAC-SHA256 هش می‌شوند.

ستون‌های قدیمی شماره تلفن در D1 عمداً حذف فیزیکی نشده‌اند تا migration مخرب یا از دست رفتن اطلاعات قبلی اتفاق نیفتد؛ اما در UI و تمام مسیرهای احراز هویت این نسخه، شماره تلفن/SMS استفاده نمی‌شود.

## داده‌های حساب

علاقه‌مندی‌ها، سبد خرید و سابقه درخواست سفارش به حساب مشتری متصل می‌شوند و صفحه `account.html` آن‌ها را نمایش می‌دهد.
